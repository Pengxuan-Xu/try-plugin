{
  "name": "try-plugin",
  "version": "0.1.1",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "try-plugin",
    "manifest": "src/manifest.json",
    "main": "try-plugin.sketchplugin",
    "assets": [
      "assets/**/*"
    ]
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.2"
  },
  "author": "Pengxuan <pumbaa5@gmail.com>",
  "dependencies": {
    "skpm": "^1.1.9"
  },
  "description": "tryout procedure",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pengxuan-xu/try-plugin.git"
  }
}
